"use client";
import "@/assets/images/logo.svg";

interface HeaderPrpos {
    title? : string;
    descript?: string;
}

export default function HeaderHome({title, descript} : HeaderPrpos){

    return (
        <header>
            
        </header>
    );
}